events {
    worker_connections 1024;
}

http {
    upstream fastapi {
        server fastapi:8080;
    }

    # HTTP server (port 80) - serves everything for now
    server {
        listen 80;
        server_name 54.82.205.217;
        
        # Serve SSL validation files
        location /.well-known/ {
            root /var/www/html;
            try_files $uri $uri/ =404;
        }
        
        # Proxy all other requests to FastAPI
        location / {
            proxy_pass http://fastapi;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # HTTPS server (port 443) - commented out until SSL certificates are available
    # Uncomment and configure when you have SSL certificates
    # server {
    #     listen 443 ssl;
    #     server_name 54.82.205.217;
    #     
    #     # SSL configuration
    #     ssl_certificate /etc/nginx/certs/cert.pem;
    #     ssl_certificate_key /etc/nginx/certs/key.pem;
    #     
    #     # Serve SSL validation files on HTTPS
    #     location /.well-known/ {
    #         root /var/www/html;
    #         try_files $uri $uri/ =404;
    #     }
    #     
    #     # Proxy all other requests to FastAPI
    #     location / {
    #         proxy_pass http://fastapi;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
